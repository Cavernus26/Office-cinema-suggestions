rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Explicitly allow everything for now to bypass any hidden auth issues
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
